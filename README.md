Implemented a FastAPI-based backend service with Docker support.
Includes health checks, secure webhook ingestion with HMAC validation,
SQLite-based message storage, message listing, and stats endpoints.
